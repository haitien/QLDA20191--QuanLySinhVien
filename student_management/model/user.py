class User(object):
    def __init__(self, params):
        pass

    def login(user_name, password):
        login_query = "select * from User where userName=@user_name, password=@password"
        pass
